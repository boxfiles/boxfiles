# @boxfiles/provider-permissions

Permissions action provider placeholder for Boxfiles.

This package reserves the `permissions` plugin capability for future file mode and access-control changes.

Expected future actions may include chmod-style mode changes, executable-bit management, and platform-specific permission handling.

## Status

No actions are implemented yet. The package currently registers an empty `permissions` plugin so manifests and package boundaries can stabilize before implementation.
